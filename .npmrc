# NPM Configuration to fix CI/CD build issues
optional=false
audit=false
fund=false
progress=false
loglevel=error

# Skip problematic scripts
ignore-scripts=false

# Registry settings
registry=https://registry.npmjs.org/

# Cache settings
cache-min=86400

# Resolve dependency conflicts
legacy-peer-deps=false
strict-peer-deps=false
