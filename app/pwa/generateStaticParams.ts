// Add this import at the top if not already there
import Link from 'next/link';

// Then add this invisible link anywhere in your JSX (like in the header)
<Link href="/pwa" className="hidden">PWA</Link>
